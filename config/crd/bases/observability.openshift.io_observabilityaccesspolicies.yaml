---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.3
  name: observabilityaccesspolicies.observability.openshift.io
spec:
  group: observability.openshift.io
  names:
    categories:
    - observability
    kind: ObservabilityAccessPolicy
    listKind: ObservabilityAccessPolicyList
    plural: observabilityaccesspolicies
    shortNames:
    - oap
    singular: observabilityaccesspolicy
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ObservabilityAccessPolicy is the Schema for the observabilityaccesspolicies
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ObservabilityAccessPolicySpec defines the desired state of
              ObservabilityAccessPolicy.
            properties:
              permissions:
                description: Permissions is a list of permission rules granting access.
                items:
                  description: PermissionRule defines a specific set of permissions.
                  properties:
                    namespaces:
                      description: |-
                        Namespaces is a list of Kubernetes namespaces.
                        Only applicable and used when resourceScope is "application".
                        If omitted or empty for 'application' scope, implies all namespaces in the specified tenants for this rule.
                      items:
                        type: string
                      type: array
                    permission:
                      default:
                      - read
                      description: |-
                        Permission defines the levels of access granted.
                        Defaults to ["read"].
                      items:
                        description: AccessLevelType defines the level of access.
                        enum:
                        - read
                        - write
                        type: string
                      type: array
                    resourceScope:
                      default: application
                      description: |-
                        ResourceScope defines the scope of the observability data this permission applies to.
                        Defaults to "application".
                      enum:
                      - application
                      - infrastructure
                      - audit
                      type: string
                    signals:
                      description: |-
                        Signals lists the allowed signal types (metrics, logs, traces) for the scope.
                        Only applicable if resourceScope is "application".
                      items:
                        description: SignalType defines the type of observability
                          signal.
                        enum:
                        - metrics
                        - logs
                        - traces
                        type: string
                      type: array
                    tenants:
                      description: |-
                        Tenants is a list of tenant IDs this permission rule applies to.
                        Required for all scopes: 'application', 'infrastructure', and 'audit'.
                        Can use "*" for all tenants (use with caution, interpretation handled by OPA policy).
                      items:
                        type: string
                      minItems: 1
                      type: array
                  required:
                  - tenants
                  type: object
                minItems: 1
                type: array
              subjects:
                description: Subjects is a list of users, groups, or service accounts
                  this policy applies to.
                items:
                  description: Subject represents a user, group, or service account
                    that can be granted permissions.
                  properties:
                    apiGroup:
                      default: rbac.authorization.k8s.io
                      description: |-
                        APIGroup is the API group of the subject.
                        Defaults to "rbac.authorization.k8s.io" if not specified for Kind Group.
                      type: string
                    kind:
                      description: Kind of the subject (User or Group).
                      enum:
                      - User
                      - Group
                      type: string
                    name:
                      description: Name of the subject.
                      minLength: 1
                      type: string
                  required:
                  - kind
                  - name
                  type: object
                minItems: 1
                type: array
            required:
            - permissions
            - subjects
            type: object
          status:
            description: ObservabilityAccessPolicyStatus defines the observed state
              of ObservabilityAccessPolicy.
            properties:
              conditions:
                description: Conditions of the access policy.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
            type: object
        type: object
        x-kubernetes-validations:
        - message: Name must be a valid DNS1035 label
          rule: self.metadata.name.matches('^[a-z][a-z0-9-]{1,61}[a-z0-9]$')
    served: true
    storage: true
    subresources:
      status: {}
